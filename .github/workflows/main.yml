name: Docker Image CI
on:
  push:
    branches:
    - master
    - gh-actions
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/docker/cli@master
    - name: Build the Docker image
      run: |
        ls -la #docker build -f Dockerfile -t robertd/alpine-terraform .
        ls -la ../
